# Mock RabbitMQ Plugin for Tests

This plugin is used by CLI tools test suites.

## Building

``` shell
gmake dist DIST_AS_EZ=1
```

then find the `.ez` file under `./plugins`:

``` shell
ls -lha ./plugins
```

## License

See [LICENSE](./LICENSE).
